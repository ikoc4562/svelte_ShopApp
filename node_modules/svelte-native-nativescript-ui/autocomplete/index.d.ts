import { NativeViewElementNode } from "svelte-native/dom";
import { RadAutoCompleteTextView, TokenModel } from 'nativescript-ui-autocomplete';
export default class RadAutoCompleteTextViewElement extends NativeViewElementNode<RadAutoCompleteTextView> {
    constructor();
    private loadView;
    private updateListItem;
    addToken(token: TokenModel): any;
    insertTokenAtIndex(token: TokenModel, index: number): any;
    removeAllTokens(): any;
    removeToken(token: TokenModel): any;
    removeTokenAtIndex(index: number): any;
    resetAutoComplete(): any;
    tokenAtIndex(index: number): any;
    tokens(): any;
    static register(): void;
}
